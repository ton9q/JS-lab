import { HighlightVideoDirective } from './highlight-video.directive';

describe('HighlightVideoDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightVideoDirective();
    expect(directive).toBeTruthy();
  });
});
